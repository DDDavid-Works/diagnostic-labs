<Window x:Class="DiagnosticLabs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiagnosticLabs"
        xmlns:bll="clr-namespace:DiagnosticLabsBLL.Globals;assembly=DiagnosticLabsBLL"
        mc:Ignorable="d"
        Title="MainWindow" WindowState="Maximized" Icon="Images\labs.ico" Closed="Window_Closed">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="{Binding CompanySetup.CompanyName}"></Label>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2">
            <Label Content="Current User:"></Label>
            <Label Content="{Binding User.Username}"></Label>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <ItemsControl x:Name="ModulesMenu2">
                <ItemsControl.ItemTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type bll:MenuItem}" ItemsSource="{Binding Items}">
                        <Button>
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{Binding Icon}"></Image>
                                <TextBlock FontSize="15" Margin="2,2,0,0" Text="{Binding Title}" />
                            </StackPanel>
                        </Button>
                    </HierarchicalDataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        <TreeView Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="1" x:Name="ModulesMenu">
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <EventSetter Event="MouseDoubleClick" Handler="TreeViewItem_MouseDoubleClick"></EventSetter>
                    <EventSetter Event="KeyDown" Handler="TreeViewItem_KeyDown"></EventSetter>
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type bll:MenuItem}" ItemsSource="{Binding Items}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Icon}"></Image>
                        <TextBlock FontSize="15" Margin="2,2,0,0" Text="{Binding Title}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Window>
